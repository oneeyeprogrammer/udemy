// 1. Debugger
const createFullName = (firstName, middleName, lastName)=>{
    debugger
    const fullName = `${firstName} ${lastName} ${lastName}`
    return fullName
}

console.log(createFullName(`Usep`, `Syaikal`, `Arifin`))