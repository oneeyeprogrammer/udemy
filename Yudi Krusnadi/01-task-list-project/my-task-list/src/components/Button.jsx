/* Button Component */
const Button = ({ textButton }) => {
    return (
        <div className="button-container">
            <button>{textButton}</button>
        </div>
    );
};

export default Button;
